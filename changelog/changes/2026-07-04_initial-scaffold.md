# 2026-07-04 Initial scaffold

Files changed:
- Created frontend files under `frontend/` (index.html, app.js, style.css, PWA files, pages)
- Created backend files under `backend/` (main.py, db.py, models.py, requirements.txt)
- Added `caddy/Caddyfile` for reverse proxy
- Added `docs/README.md`
- Added changelog entries

How the change was made:
- Generated scaffold files containing minimal working templates and configuration.

Why the change was necessary:
- Provide a starting point for development implementing offline-first PouchDB frontend and FastAPI backend.

Impact:
- Base project structure ready. Developers can run and extend components.
